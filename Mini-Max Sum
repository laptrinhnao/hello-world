#include <iostream>

using namespace std;

int main() 
{
    long long a[5], max,min,s=0,s1=0,s2=0;
    for(int i=0;i<5;i++)
    {
        cin>>a[i];
    }
    for(int i=0;i<5;i++)
    {
        s=s+a[i];
    } 
    max=a[0];
    min=a[0];
    for(int i=1;i<5;i++)
    {
        if(a[i]>max)
        {
            max=a[i];
        }   
        if(a[i]<min)
        {
            min=a[i];
        }
    }
    s1=s-max;
    s2=s-min;
 cout<<s1<<" "<<s2;
    return 0;
}
    
